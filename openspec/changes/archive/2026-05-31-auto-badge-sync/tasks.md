## Tasks

- [x] Change AUTO ⏵⏵ indicator color from red to #FFCC00 in `SessionIdentityLine` (`NotchPanelView.swift`)
- [x] Display `session.permissionMode` as secondary text in `SessionIdentityLine` when available
- [x] Add bidirectional sync logic in `AppState.handleEvent` to activate/deactivate AUTO based on hook-reported `permission_mode`
- [x] Build and verify Debug app
- [x] Build Release app and bump version to 1.2.6
